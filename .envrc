# Auto-load the flake's dev shell with direnv (`direnv allow`).
# Requires nix-direnv for caching: https://github.com/nix-community/nix-direnv
if ! has nix_direnv_watch_file; then
  watch_file flake.nix flake.lock nix/package.nix nix/module.nix
fi
use flake
